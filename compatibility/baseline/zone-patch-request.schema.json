{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.sudo.dev/schemas/auth/v1/zone-patch-request.schema.json",
  "title": "ZonePatchRequest",
  "description": "Whitelisted-field patch. This is deliberately NOT Partial<Zone>: immutable identity (zone_id, origin trust domain, created_by) never appears, and unknown keys are rejected rather than ignored — a patch naming anything outside the whitelist is a client bug, not a forward-compatibility case.",
  "type": "object",
  "properties": {
    "api_version": {
      "const": "auth.sudo.dev/v1"
    },
    "kind": {
      "const": "ZonePatchRequest"
    },
    "display_name": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string"
    },
    "labels": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "deployment": {
      "type": "object",
      "description": "Partial placement update; whitelisted keys only — trust_domain is immutable and absent on purpose.",
      "properties": {
        "region": {
          "type": "string",
          "minLength": 1
        },
        "data_domain": {
          "enum": [
            "office",
            "core",
            "general"
          ]
        }
      },
      "additionalProperties": false,
      "minProperties": 1
    }
  },
  "required": [
    "api_version",
    "kind"
  ],
  "additionalProperties": false
}
